---
import { buttonVariants } from './ui/button'
import { cn } from '../lib/utils'

export interface Props {
    size: 'sm' | 'md' | 'lg'
}
const { size } = Astro.props
---

<a
    href="https://thetismedical.myshopify.com/"
    target="_blank"
    class={buttonVariants({ variant: 'default', size: size })}>Shop</a
>
