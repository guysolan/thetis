---
import GuideLayout from "@/layouts/GuideLayout.astro";
import { metadata, content } from "@/content/guide/plantar-fasciitis/stage-3";
import { plantarFasciitisGuideNavPhases } from "@/content/guide/plantar-fasciitis";
---

<GuideLayout
 title={metadata.title}
    description={metadata.description}
    slug={`guide/plantar-fasciitis/${metadata.slug}`}
    lang="en"
    content={content}
    navPhases={plantarFasciitisGuideNavPhases}
/>
