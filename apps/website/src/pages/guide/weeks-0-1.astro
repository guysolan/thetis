---
import GuideLayout from "@/layouts/GuideLayout.astro";
import { metadata, content } from "@/content/guide/week-0-1";
---

<GuideLayout
    title={metadata.title}
    description={metadata.description}
    slug={`guide/${metadata.slug}`}
    lang="en"
    content={content}
/>
