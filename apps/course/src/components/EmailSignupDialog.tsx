// Re-export the shared EmailSignupDialog from @thetis/ui
// This allows us to use a consistent component across all apps
// and optionally wrap it with app-specific logic in the future

export { EmailSignupDialog } from "@thetis/ui/email-signup-dialog";
export type { EmailSignupDialogProps } from "@thetis/ui/email-signup-dialog";
