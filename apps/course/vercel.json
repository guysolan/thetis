{
  "ignoreCommand": "git rev-list --count HEAD | grep -q '^1$' && exit 1 || git diff origin/main...HEAD --quiet -- apps/course/ packages/ui/ turbo.json package.json bun.lockb",
  "git": {
    "deploymentEnabled": {
      "main": true,
      "course": true,
      "dev": false,
      "preview": false
    }
  }
}
